@model IEnumerable<DHMS01.Models.At>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AtName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AtName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AtId }) |
            @Html.ActionLink("Details", "Details", new { id=item.AtId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AtId })
        </td>
    </tr>
}

</table>
