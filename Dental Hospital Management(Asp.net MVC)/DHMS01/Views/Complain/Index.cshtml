@model IEnumerable<DHMS01.Models.Complain>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ComplainName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ComplainName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ComplainId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ComplainId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ComplainId })
        </td>
    </tr>
}

</table>
